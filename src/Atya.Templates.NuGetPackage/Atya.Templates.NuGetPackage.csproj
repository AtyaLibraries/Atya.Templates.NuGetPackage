<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>Atya.Templates.NuGetPackage</AssemblyName>
    <PackageId>Atya.Templates.NuGetPackage</PackageId>
    <RootNamespace>Atya.Templates.NuGetPackage</RootNamespace>
    <IsPackable>true</IsPackable>

    <!-- MinVer derives the version from git tags (e.g. tag `v1.2.3` -> 1.2.3). -->
    <MinVerTagPrefix>v</MinVerTagPrefix>
    <MinVerDefaultPreReleaseIdentifiers>alpha.0</MinVerDefaultPreReleaseIdentifiers>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
    <None Include="..\..\LICENSE" Pack="true" PackagePath="\" Visible="false" />
  </ItemGroup>

  <ItemGroup Label="Runtime dependency + analyzers + versioning">
    <PackageReference Include="Atya.Foundation.Guards" />
    <PackageReference Include="Atya.Governance.CodeQuality" PrivateAssets="all" />
    <PackageReference Include="MinVer" PrivateAssets="all" Condition="'$(RepositoryHasGitDirectory)' == 'true' And '$(IsTemplateSourceRepository)' != 'true'" />
    <PackageReference Include="Microsoft.SourceLink.GitHub" PrivateAssets="all" Condition="'$(RepositoryHasGitDirectory)' == 'true' And '$(RepositoryUrlIsConfigured)' == 'true' And '$(IsTemplateSourceRepository)' != 'true'" />
  </ItemGroup>

</Project>
